{
  "name": "travel-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "firebase": "^12.3.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.1",
    "fake-indexeddb": "^6.2.2",
    "vite": "^7.1.7",
    "vitest": "^5.0.1"
  }
}
